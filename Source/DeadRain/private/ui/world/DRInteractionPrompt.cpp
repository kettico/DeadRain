#include "ui/world/DRInteractionPrompt.h"

#include "interaction/DRInteractableActor.h"
void UDRInteractionPrompt::SetInteractable(ADRInteractableActor* IA){
    if (!IA) {
        // Clear Prompt and make invisible
    }

    // Update text 
}